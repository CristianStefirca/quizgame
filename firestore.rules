rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /progress/{doc} {
      allow read, write: if true;
    }
    match /activeRun/{doc} {
      allow read, write: if true;
    }
  }
}
